# Changelog

## Unreleased — 학습 힌트가 "실제 사용값" 표시 (혼란 수정)

- **"인식:" 힌트가 거짓말하던 문제 수정**: 값이 잠금/거부 상태여도 힌트가 *무시되는 라이브 OCR*을 표시 → "내가 입력한 값과 다른 값을 쓰는 것처럼" 보였음. 실제로는 앱이 입력값을 정상 사용 중이었음. 이제 힌트는 **앱이 실제 사용하는 값**을 표시하고(`인식:`=채택 / `사용: X · OCR Y(무시)`=거부·잠금), 무시되는 OCR 읽기는 진단용으로 함께 노출. 수동 입력 즉시 반영.
- **ROI 편집기 "직접 지정됨" 상태 표시**: 어떤 항목에 수동 영역이 지정됐는지 한눈에 확인.
- **아데나 쉼표 안내**: 화면이 `31,525`처럼 쉼표를 포함하면 학습 시 쉼표까지 입력해야 글자수가 맞음(안내 + 예시 추가). 학습 실패 메시지를 정직하게(실패 사유 노출, "정상입니다" 오문구 제거).

## Unreleased — 창 모드 영역 직접 지정 편집기

- **영역 직접 지정(ROI) 편집기 추가** (창 모드, 설정 → 화면 인식 → 「🎯 영역 직접 지정」): 「📷 게임 화면 불러오기」로 게임 창 스냅샷을 띄우고, 항목(MP 숫자/MP 바/EXP/레벨/아데나)을 골라 이미지 위에서 마우스로 드래그하면 해당 영역을 직접 지정. 지정한 영역은 자동 검출 대신 그대로 사용됨(`autoDetect.windowRoi` 오버라이드, 창-프레임 물리px 1:1). 자동 ROI가 EXP/레벨/아데나를 잘못 잡던 클라이언트도 MP처럼 정확하게 만들 수 있음. 「선택 영역 자동으로」로 개별 해제.
- 스냅샷은 창 네이티브 해상도로 1:1 표시(스크롤) → 그린 좌표가 곧 캡처 좌표라 오차 없음. 저장 즉시 detection이 재반영(`forceRoiRefresh`).
- 스키마 v2→v3 마이그레이션(`windowRoi`), 기존 사용자 무영향. 사용 흐름: 영역 지정 → 「보정·학습」에서 글자 학습 → 정확.

## Unreleased — 수동 입력값 잠금 (OCR이 덮어쓰던 문제)

- **직접 입력한 값을 OCR이 즉시 덮어쓰던 문제 수정**: `force`(수동 입력)에 시간 잠금이 없어 다음 틱의 OCR(오인식 포함)이 곧바로 덮어썼음. 이제 EXP/레벨/아데나는 수동 입력 후 **10분간 OCR 무시**(값 고정), MP는 제외(bar-pixel이 정확하고 MP는 계속 변함). tracker에 `manualLockMs` + `user_locked` reason 추가, 단위 테스트 3종.
- **알려진 한계**: 창 모드 자동 ROI가 일부 클라이언트에서 EXP/레벨/아데나 영역을 잘못 잡음(레벨 6글자·아데나 10글자 등) → 자동 인식 부정확. 근본 해결은 "게임 창에 영역 직접 지정 편집기"(예정). 그 전까지는 수동 입력(이제 10분 고정)으로 사용 가능. MP는 자동 정확.

## Unreleased — 단축키 비활성화 버그 수정

- **전역 단축키(F1/F2) 끄기가 적용 안 되던 버그 수정**: ① main이 시작 시 `DEFAULT_HOTKEYS`(F1/F2 ON)를 무조건 등록하는데 렌더러가 **부팅 시 저장된 enable/disable 상태를 동기화하지 않아**, 껐다가 재시작하면 다시 켜졌음 → 부팅 시 `setGlobalHotkeys` 동기화 추가. ② `setGlobalHotkeys` 재등록이 `pushEvent('hotkey')`로 라우팅됐는데 렌더러는 이 이벤트를 구독하지 않아 재등록된 F1/F2가 무반응이 되던 잠재 버그 → main-side `dispatchHotkey`로 일원화(시작·재등록 동일 경로).
- **레벨 수동 입력 안내 정리**: 창 모드 자동 ROI가 레벨 영역을 과분할(6글자)해 자동 인식이 어려운 점을 반영 — 레벨은 보정·학습에서 직접 입력하면 OCR 오인식이 덮어쓰지 않고 유지됨(메시지를 정직하게 수정).

## Unreleased — UI 가독성/폰트 + EXP 표시 정리

### UI
- **입력칸 텍스트 안 보임 수정**: 보정·학습/아이템 입력칸이 `.field` 밖(`.row`)이라 테마 미적용 → UA 흰 배경 + 밝은 글자(=안 보임)였음. 전역 폼 컨트롤 테마(어두운 배경·밝은 글자·placeholder·focus, WCAG AAA 대비)로 수정. 키보드 `:focus-visible` 링은 유지(a11y).
- **버튼 크기 축소**: `.btn` 패딩 8/16→8/12, 글자 15→13px(`--sm` 11px), nowrap — 전반적으로 덜 투박하게.
- **보정·학습 행 간격**: `.card` 직속 행들이 세로로 붙던 문제 → `.stack` 유틸 + 학습행 2줄 구조(라벨+입력+버튼 / 상태+힌트), 입력칸 flex.
- **폰트 교체**: 시스템 폴백(맑은 고딕) → **Pretendard**(variable woff2 번들, 오프라인). 빌드 전용(devDependency)이라 exe 크기 영향 최소(+2MB).

### EXP 표시/학습
- **`인식:` 힌트가 정규화값 표시**: raw `79.3390232`(끝 "%"가 3조각 과분할) 대신 파서 정규화값 `79.3390` 표시(raw는 툴팁). 추적값은 원래부터 정상이었고 표시만 raw여서 생긴 혼란 해소.
- **EXP 학습 과분할 허용**: `learnFromCapture`가 EXP 한정 좌측 N글자만 학습(숫자는 좌측, "%" 노이즈는 우측). 다른 영역은 strict 유지.
- 창 모드 학습 실패 안내 문구 정정(영역 재지정 불가 → "자동 영역, 값 인식 정상").
- `인식값↩`: EXP는 정규화값, 그 외는 raw(콤마/슬래시 글자수 보존)로 채움.

## Unreleased — 듀얼 모니터 핫픽스 + 게임 창 자동 캡처

### 듀얼 모니터 / 고배율(HiDPI) 캡처 버그 수정
- **이중 스케일링 제거** (`screen-capture.ts`): 오버레이가 영역 지정 시 이미 논리→물리 픽셀로 변환해 저장하는데, 캡처 시 `scaleFactor`를 한 번 더 곱해(`region.x * sf`) 배율 ≠ 100% 디스플레이에서 크롭이 빗나가던 버그. 100% 모니터는 무해(1×1)했으나 125/150% 보조 모니터에서 영역이 어긋나 OCR 실패. 저장 좌표를 물리 픽셀로 통일하고 캡처 시 재곱셈 제거.
- 데스크톱 캡처 해상도 상한 4096/2160 → **7680/4320** (4K↑/듀얼-4K에서 다운스케일로 물리=비디오 가정이 깨지던 위험 제거).

### 게임 창 자동 캡처 모드 (모니터 무관)
- **캡처 방식 선택**: 「화면 영역(수동)」 ↔ 「게임 창(자동)」. 창 캡처는 영상 원점이 창 좌상단이라 **어느 모니터에 있든, 창을 옮겨도** ROI가 유효 — 듀얼 모니터 좌표 매칭 문제 자체가 사라짐.
- **창 제목 자동 매칭**: `listWindows`로 창 목록 → `lineage|리니지` 자동 선택. 창 `window:HWND` id는 게임 재시작마다 바뀌므로 매 시작 시 **제목으로 재해결**(`resolveWindowSource`).
- **자동 ROI 검출**: `detectGameUiScaled`가 창 전체 프레임에서 HP/MP/EXP/레벨/아데나 ROI를 배율 정규화(기준 1280폭 다운스케일→검출→원본 px 복원)로 자동 도출. 창 ROI는 메모리 보관(영속 스토어 미오염, scaleFactor=1).
- **견고성**: 검은 화면(전체화면 독점) 감지 + 안내, 창 최소화/소실 시 `roiFailThreshold` 초과하면 자동 재탐색(self-heal), 「게임 창 탐색 중…」 상태 표시.
- 저장 스키마 v1→v2 마이그레이션(`captureMode`/`windowId`/`windowTitle`), 기존 화면-영역 사용자 무영향.
- 신규 IPC: `listWindows`, `resolveWindowSource`. 신규 테스트: 합성 풀프레임 ROI 검출(1×/2× 배율·디코이·얇은 EXP·검은 프레임), 스키마 마이그레이션.

## v3.0.0 — 2026-06-05

전면 리뉴얼. JavaScript 모놀리식(8268줄 `app.js`) → **TypeScript + electron-vite** 모듈 구조.

### OCR(문자 인식) 근본 재설계
v2.x의 간헐 오인식(`0↔8`, `4↔9`, `6↔8`) 근본 원인을 제거:

- **등폭 글자 분할 폐기 → 연결성분(CCL) 분할**: 잉크의 실제 연결 구조로 글자 경계를 찾음 (한 컬럼 어긋남이 옆 글자를 오염시키던 #1 원인 제거)
- **MP 바 픽셀 측정**을 MP 1순위로: 게이지 채움 컬럼 수 직접 측정 → ~100% 정확, OCR 불필요 (100% 보정 1회 필요)
- **유저 디지트 템플릿 학습**: 실제 값 입력 → 본인 게임 폰트 학습 → 해당 글자는 항상 정확 (학습된 글자는 기본 템플릿으로 폴백 안 함)
- **베이지안 시간추적 거부권**: v2.x DRY-RUN(관찰만) → 실제 거부권 부여
- **EXP 소수 4자리 정규화**: 끝의 `%` 글자가 가짜 5번째 소수로 잡히던 문제 자동 보정 (`78.47472` → `78.4747`)
- 캔버스/DOM 독립 `RgbaImage` 파이프라인 → 게임 없이 1,300여 캡처 샘플로 정확도 단위 테스트

### UI/UX
- 4탭(개발자 콘솔 노출) → **3탭** (모니터 / 설정·OCR / 환경설정)
- 디자인 토큰 시스템 (v2.x 미정의 `--accent` 등 해결, 시각언어 통일)
- 카운트다운 히어로 중심 시각 계층, 진단은 숨김 드로어로
- 접근성: ARIA, `prefers-reduced-motion`, 테마 스크롤바
- 컴팩트 HUD 재설계

### 진단
- 중앙 로그 시스템 (`고급·진단` 드로어 / DevTools `lmpLog`), 로그 복사·저장
- 영역별 인식 결과(원문/파싱/채택/거부사유) 실시간 기록

### 인프라
- 단일 reactive store (DOM-as-anchor 안티패턴 제거)
- 타입 안전 IPC 계약, Electron main 8개 모듈 분리, async fs
- 저장 스키마 버전 관리(ad-hoc 마이그레이션 플래그 제거)
- vitest 112개 테스트, node/web 타입체크 통과

### 정리
- 구버전 빌드 산출물(v1.8.4, win-unpacked ~400MB) + 죽은 스크립트 + 모놀리식 소스 제거
- Paddle/Tesseract voting 은퇴, 미사용 traineddata 번들 제외
