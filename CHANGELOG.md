# Changelog

## v3.1.8 — 2026-06-13

### MP를 틱 모델로 예측 — 카운트다운 매끄러움 + 표시값 노이즈 제거 (`mp-timer.ts`)

실사용 잔여 문제 2건: ① MP 표시값이 흔들려 정확도 부족(예: 6↔13, 21↔13), ② 완충 카운트다운이 실시간으로 1초씩 줄지 않음. 근본 원인은 동일 — **MP 바-픽셀 측정이 프레임마다 ±노이즈로 흔들려 `cur`이 단조 증가하지 않음**. v3.1.7의 `anchorSecs`(정확 ETA 일치) 가드는 노이즈가 16초 틱 버킷 경계를 넘나들면 무력화돼, 매 프레임 OCR로 카운트다운을 몰면 계속 재앵커됨.

사용자 제안("MP 1틱당 계산")을 그대로 채택 — MP는 결정론적 틱(예: +N/16s)으로 회복하므로 매 프레임 측정 대신 **틱 모델로 예측**:

- **틱 예측 모델**: 앵커 `(anchorMp, anchorAt)` + 라이브 회복량/주기에서 `predictedMp(t) = anchorMp + floor(경과/주기) × 회복량`(maxMp 상한). 완충 시각이 고정되므로 카운트다운이 매끄럽게 **1초/초** 감소.
- **데드밴드 재앵커**: 측정값이 예측에서 **1틱 이상** 벗어날 때만 재앵커 — 실제 MP 사용(하락) / 물약(상승) / 회복률 변경(버프·상태·위치·WIS). 1틱 이내 지터는 무시 → 카운트다운이 되돌아가지 않음.
- **전진 위상 잠금**: 깨끗한 틱 전진(`cur ≥ anchorMp + 회복량`)이 관측되면 앵커를 전진시켜 표시 MP가 게임의 실제 틱 위상과 일치하도록 동기화하되, 완충 시각은 **앞으로만**(단조) 당김 — 노이즈로 되밀리지 않음.
- **표시 MP = 예측값**: 모니터·요약바·컴팩트 HUD가 `displayMp`(틱 예측)로 표시 → 게임의 이산 회복과 동일한 깔끔한 틱 단위, 매 프레임 측정 노이즈 제거. 타이머 정지 시엔 raw 측정값.
- 회귀: `test/domain/mp-timer.test.ts`에 틱 모델 6종 추가(틱당 정확 증가·max 상한·서브틱 지터에도 단조 1초/초·실사용 시 재앵커 상승·물약 재앵커 하락·회복률 변경 재앵커). 기존 8종 + 전체 **40파일 304개 PASS** · typecheck PASS.

> 측정 자체의 계통 오차가 아니라 프레임 노이즈가 원인이므로, 표시·카운트다운을 결정론적 틱 모델로 분리해 해결. raw 측정은 store에 유지(재앵커 근거·진단·영속).

## v3.1.7 — 2026-06-13

### 완충 카운트다운이 매 틱 리셋되던 문제 수정 (`mp-timer.ts`)

실사용: 카운트다운이 1초씩 줄지 않고 "1 줄었다 다시 늘었다" 반복하다가 MP가 한 틱 차야 진행. 원인: `app.ts`가 **모든 store 변경마다 `timer.recompute()`를 호출**하는데, recompute가 매번 `completionAt = now + ETA`로 **재앵커**했음. 검출기가 안정적인 MP를 매 ~1초마다 재수용(+EXP/아데나 샘플 기록)해 store가 계속 바뀌므로, 카운트다운이 250ms 렌더 사이에만 줄다가 재검출 때 ETA로 되돌아가고, MP가 실제로 회복돼 ETA가 줄 때만 순(net) 진행했음.

- **ETA가 실제로 바뀔 때만 재앵커**: `anchorSecs`로 직전 ETA를 기억해, 동일하면 기존 `completionAt`을 유지(매끄럽게 카운트다운) → 실제 MP 변화로 ETA가 달라질 때만 새 값으로 재앵커. start/pause 시 앵커 리셋.
- 회귀: `test/domain/mp-timer.test.ts`에 2종 추가(안정 MP에서 매 틱 recompute해도 1초/초로 감소·리셋 안 됨, MP 변화 시 새 ETA로 재앵커). 8종 전체 + 테스트 40파일 298개 PASS · typecheck PASS.

> MP 값 자체는 정확히 추적 중(로그: 98→140→161, 회복률 config 19/16s와 일치, 이전 실측 260→261). 큰 변화 시 1~2초 지연은 트래커의 오인식 방어(`low_posterior`)이며 측정 오류가 아님.

## v3.1.6 — 2026-06-13

### MP 바 측정 가득참 고착 수정 — 얇은 밴드 컬럼 밀도 (`bar-fill.ts`)

실기 진단: 게임 MP가 **260/327**인데 앱은 **327/327로 고착**(실시간 변동 안 보임). 실측 프레임 측정으로 원인 확정 — MP 게이지의 파랑-우세 밴드가 **~7px로 얇고** 보정이 ROI를 그만큼 축소하는데, `computeBarFill`의 `minColumnDensity` 기본값 0.3이 6px ROI에서 `need = floor(6×0.3) = 1`을 만들어, **빈 컬럼 상단의 광택(sheen) 픽셀 1~2개만으로 컬럼 전체를 "채움"으로 판정** → 빈 구간도 채움으로 세어 항상 full.

- **`minColumnDensity` 기본값 0.3 → 0.5**: 컬럼이 "채움"이 되려면 행의 과반이 채움색이어야 함 → 상단 1~2px 광택 무시, 실제 채움 컬럼(전 행 채움)만 계수. 실측 프레임 검증: 6px ROI에서 327(고착)→**261**(실제 260) 정확 측정. **기존 보정값(254 cols)으로 작동 → 재보정 불필요.**
- 회귀: `test/ocr/bar-fill-thin-band.test.ts`(얇은 밴드+상단 광택에서 60% 채움을 full로 오측정하지 않음, 옛 0.3은 오측정함을 문서화). 합성 보정 테스트는 채도 게이트 검증이 목적이라 `minColumnDensity:0.3` 고정으로 분리. 테스트 41파일 297개 PASS · typecheck PASS.

> 진단 경위: MP raw가 86틱 내내 "327/327"(src=bar-pixel)로 고정 → 실기 캡처 시 게임은 260/327 → 저장 보정(254, refColor 122,124,149)으로 직접 측정하니 6px ROI는 full, 13~16px 밴드는 정상(263) → 밀도 need 임계 문제로 특정.

## v3.1.5 — 2026-06-13

### MP 완충 알림 잔여 케이스 2건 수정 (`mp-timer.ts`)

실기 진단 로그로 확인: v3.1.4에서 인식은 전부 정상(MP 327·EXP 57.2%·레벨 33·아데나 20612 모두 ACCEPT)이나, **MP가 가득 찬 상태에서 시작을 누르면 "완충" 알림이 반복**되는 문제 잔존. v3.1.3 수정(가득참 동안 재발화 차단)이 못 막은 두 경로:

- **시작 시 이미 가득 참 → 즉시 알림 억제**: "완충" 알림은 MP가 *회복하여* 가득 찬 순간을 위한 것인데, 가득 찬 상태로 Start를 누르면(회복 없음) 즉시 알림이 떴음. `start()`에서 시작 시 MP가 full이면 `notified=true`로 두어 억제 — 실제로 MP를 쓴 뒤 다시 차면 그때 1회 알림.
- **가득참 근처 1픽셀 흔들림 → 재발화 차단**: MP 바 측정이 full 근처에서 327→326→327로 1단위 흔들리면(로그상 326↔327 전이 확인), 326(=full 아래)에서 재무장→327에서 재발화가 매 사이클 반복됐음. 재무장을 "의미 있는 MP 소모"(`cur ≤ max − max(2, ⌈max×2%⌉)`, 327 기준 7MP)일 때만 하도록 변경 — 측정 지터(±1~2MP)는 무시, 실제 시전(7MP+)만 다음 완충 시 1회 알림.
- 불변식 유지: **실제 리필 사이클당 정확히 1회 알림**. 회귀: `test/domain/mp-timer.test.ts` 6종(시작-가득참 무알림, 지터 무시, 실사용 후 재알림, 충전 중 무알림, blocked 무알림, 카운트다운 1회). 테스트 41파일 295개 PASS · typecheck PASS.

## v3.1.4 — 2026-06-13

### 레벨 인식 실패 수정 — 실기 진단 로그 기반 (`roi-detector.ts`)

실기 진단 리포트(`%APPDATA%\LineageMPTimer\diagnostic\`) 분석으로 확정: 실행 중인 앱에서 **레벨만 매 틱 거부**(`raw="660133" conf=0.28 → reject`)되고 MP(327 accept)·EXP(57.06% accept)·adena(accept)는 정상 동작 중이었음. 원인은 v3.1.2의 레벨 트림(`refineLevelRoiToValue`)이 **절대 밝기 게이트(lum>180)** 로 글자를 찾는데, 앱의 화면 캡처(desktopCapturer)가 PrintWindow보다 어둡게 렌더링되면 글자를 못 찾아 트림이 안 되고 "LEV:33" 전체(85px)를 그대로 OCR → "660133" → parseLevel null.

- **`refineLevelRoiToValue`를 인식기와 동일한 적응형 이진화(`binarizeAuto` + `columnInk`)로 전환**: 글자 검출이 실제 OCR되는 것과 일치하므로, 인식기가 "LEV:33"을 분할할 수 있는 한(=로그의 6글자) 트림이 항상 발동. 캡처 방식·밝기 차이에 무관.
- `levelRoiMinWidth` 30 → 14: 트림된 레벨 ROI(≈24px)가 `valid:false`로 오플래그되던 것 해소.
- 회귀: `test/ocr/roi-refine.test.ts` 6종 유지(binarizeAuto 기반에서도 통과). 테스트 41파일 294개 PASS · typecheck PASS.

> 진단 로그로 확인된 정상 동작: MP는 bar-pixel로 327 정상 추적(가득 찬 상태라 카운트다운할 게 없을 뿐 — 사용 시 드롭 측정됨, 50% 가림 재현 시 165/327), EXP 57.06% 정상. adena는 값을 읽으나 자릿수 노이즈 가능성 잔존(기존 한계).

## v3.1.3 — 2026-06-13

### MP 완충 알림 무한 반복 수정 (`mp-timer.ts` / `ipc.ts`)

실사용 버그: "MP 충전 완료" 토스트+사운드가 **계속 반복**해서 울리고, MP를 써도·창을 X로 닫아도 멈추지 않음. 3방향 진단(알림재발화/측정고착/생명주기)으로 근본원인 확정.

- **근본 원인 — 완료 플래그 매 틱 리셋** (`MpTimer.recompute`): `recompute()`가 호출될 때마다 `notified=false`로 리셋했는데, `app.ts`는 **모든 store 변경마다 recompute를 호출**한다. MP가 가득 차면 `calculateFullMpTime`이 0 → `completionAt=now`가 되고, 세션 트래커가 매 틱 샘플을 store에 쓰면(store는 디듀프 없음) → recompute → `notified` 리셋 → 250ms 렌더 폴링의 `remainingSeconds`가 즉시 재발화. 무한 반복.
- **수정**: `notified`를 **`secs > 0`(MP가 full 아래인 진짜 카운트다운)일 때만** 리셋. 가득 찬 동안(secs=0)은 플래그를 유지 → 정확히 **리필 사이클당 1회** 알림(MP가 떨어지면 재무장, 다시 차면 1회 발화). "MP 사용 후에도 계속" 증상도 같이 해소(플래그가 완료 1회를 기억).
- **X 종료 후에도 지속 — 정체 규명**: X 닫기는 실제로 앱을 종료함(검출 루프는 렌더러와 함께 파괴, 트레이는 BrowserWindow 아니라 window-all-closed→app.quit 정상). "닫아도 계속"은 종료 전까지 반복 발화된 **OS 토스트(Electron 네이티브 Notification)가 Windows 알림센터에 수십~수백 개 적재**돼 남아 보이던 것. 반복 발화를 막으면 해소.
- **보강** (`notifyComplete`): 완료 토스트를 모듈 스코프 단일 핸들로 재사용 — 발화 시 이전 토스트를 `close()` 후 새로 표시해 알림센터 적재 방지(값이 full 근처에서 진동해도 안전).
- 회귀망: `test/domain/mp-timer.test.ts`(5종 — 가득참 동안 1회만, 리필당 1회, 충전 중 무발화, blocked 무발화, 카운트다운 완료 1회+재발화 없음). 테스트 40파일 293개 PASS · typecheck PASS.

## v3.1.2 — 2026-06-13

### 레벨·아데나 자동 ROI 정밀화 — 실기 프레임 기반 수정 (`roi-detector.ts`)

실기(LC 창 1296×999) 프레임을 캡처해 `detectGameUiScaled`를 직접 돌려 진단: 자동검출은 4개 앵커를 모두 찾고 ROI도 도출하지만(인식기 자체는 정상 — 픽스처로 레벨 8/8·아데나 5/8 확인), 레벨·아데나 ROI가 **잘못된 영역을 잘라** 인식에 실패하고 있었음.

- **레벨 = "LEV:" 라벨 제거** (`refineLevelRoiToValue`): 인게임 레벨 표시는 "LEV:33"으로 라벨+값이 한 시각 그룹이라, 자동 ROI가 "LEV:33" 전체를 잡아 OCR이 "LEV:"를 숫자로 오인 → "660233" → `parseLevel` null(>99)이었음(핸드오프의 "660133" 정체). ROI 컬럼의 글자 런을 스캔해 라벨↔값 간격(실측 6px)이 숫자 간 간격(3px)보다 확연히 큰 지점 중 **가장 오른쪽**에서 분리, 뒤쪽 1~3자리(값)만 남김. 라벨 없는 깨끗한 "NN" 크롭(단일 런/균일 간격)은 변경 없음.
- **아데나 = 금화 아이콘·셀 경계 제거** (`tightenToTextBBox`): 아데나 숫자는 노란 금화 아이콘 바로 아래 좁은 인벤토리 칸에 있어, 기하 박스가 금화(고채도 노랑)와 칸 경계(어두움)를 함께 잡아 유령 자릿수 발생("16476"→"115476"). ① "below" 박스 시작점을 아이콘 바닥으로 클램프(0.83×높이가 32px 아이콘을 5px 물던 것 해소) + ② **밝고 저채도(흰 숫자) 픽셀의 바운딩박스로 타이트하게 재크롭** — 금화(sat 0.98)·경계는 색 특성으로 자동 제외. 실기 프레임에서 레벨 "33"·아데나 "16476" 정확 인식 확인.
- 회귀망: `test/ocr/roi-refine.test.ts`(헬퍼 6종 — "LEV:33"/"LEV:100" 분리, 깨끗한 크롭·단일 글자 무변경, 금화/경계 제외, 텍스트 없을 때 무변경), 진단: `test/debug/dbg-live-frame-roi.test.ts`(실기 프레임 있으면 자동 ROI 덤프·재인식, 없으면 skip)·`dbg-level-adena-recog.test.ts`(픽스처로 인식기 무죄 입증). 테스트 39파일 288개 PASS · typecheck PASS.

## v3.1.1 — 2026-06-13

### MP 바 보정 영구 실패 해소 — 저채도 게이지 게이트 교정 (`bar-fill.ts` / `detection.ts` / `setup.ts`)

HANDOFF-2026-06-07 미해결 이슈의 원인 확정(합성 ROI로 필드 감지색 rgb(192,142,96)을 정수 단위까지 재현) 후 근본 수정. 실측 게이지 본체 rgb(74,74,92)의 채도는 0.196인데 `detectFillColor`의 채도 게이트가 0.25라 게이지 파랑 전 픽셀이 탈락하고, 유일하게 게이트를 통과하는 금장식(채도 0.50)만 평균돼 fillColor=갈색 → `not_blue` 영구 거부되던 구조.

- **채도 게이트 0.25 → 0.12** (`FILL_SATURATION_GATE` 상수화): 실측 게이지 채도 0.163~0.196 전 범위 수용, 흰 오버레이 텍스트(채도 ≤~0.05)는 계속 배제. 갈색 패널 거부는 파란-우세 게이트 소관이라 거짓성공 차단 불변.
- **행 밴드 픽셀 투표만 완화 — `BAND_PIXEL_VOTE_MARGIN`(신설) 8, 최종 색 게이트 `BLUE_DOMINANCE_MARGIN`은 15 유지**: 본체 파랑 마진 18 대비 투표 여유가 3뿐이라 디더링/광택 한 번에 행 밴드가 붕괴하던 것을 투표 여유 10으로 확보. 보정 수락·저장 보정 무효화·ROI freeze 판정이 공유하는 최종 게이트는 15를 유지해 v3.0.2 거짓성공 방어의 색 공간(청회색 마진 9~15·보라 등)과 레거시 무효화 의미론을 그대로 보존 — 디프 적대적 검증(3-렌즈)이 "최종 게이트까지 8로 내리면 균일 청회색/보라 ROI 거짓성공 + 구버전이 무효화한 보정의 무경고 재활성화"를 실측으로 지적해 반영. 클러스터 평균(아래)이 마진 ~18로 학습되므로 필드 케이스는 15에서도 통과(합성 실측 18.1).
- **파랑-클러스터 우선 평균** (`detectFillColor`): 게이트 통과 픽셀 중 파랑-우세(b>max(r,g)) 부분집합이 충분하고 과반이면 그 평균을 채택 — 밴드 붕괴로 금장식이 샘플에 혼입돼도(5% 혼입만으로 혼합 평균이 파랑-우세를 상실) 게이지 색을 정확히 학습. 갈색-only ROI는 클러스터가 비어 전체 평균 폴백 → 거짓성공 거부 경로 보존.
- **표본 하한 면적 비례화** (`detectFillColor`): 기존 하한 height×0.5는 샘플 면적의 ~0.7%라 게이트 미달 균일색도 노이즈 이상치 소수로 채움색이 성립하는 "복권" 경로가 있었음 — 샘플 면적의 5%로 상향(필드 게이지는 최악 케이스도 55% 통과라 무영향).
- **보정 첫-프레임 재시도** (`calibrateMpBar`): 온디맨드 창 바인딩 직후 video 미도색(videoWidth 0/검은 프레임)으로 첫 클릭이 헛돌던 race — captureWindowFrame과 동일한 8×150ms 재시도 적용.
- **무효 보정의 ROI 동결 차단** (`ensureWindowRois`): 기하 freeze 게이트가 `mpBarMaxX>0`만 보고 보정 유효성(`isBlueDominant`)을 안 봐서, 시스템 스스로 "보정 없음" 취급하는 레거시 쓰레기 보정이 첫 자동 ROI를 동결시키고 재보정이 그 잘못된 ROI를 계속 캡처하던 자기강화 교착 해소.
- **보정 stale-ROI race 수정** (`calibrateMpBar`): 창 모드 보정이 메모리 캐시만 읽어, ROI 드래그 직후 보정 클릭 시(캐시 재구축은 다음 틱) 옛 영역을 캡처하던 문제 — 영속 오버라이드를 캐시보다 우선 사용하고 캐시에 미러링. 루프 정지 중에도 창 제목으로 소스를 즉석 바인딩해 보정 가능.
- **ROI 오버라이드 증발 수정** (`setup.ts` `saveRoi`): raw `store.set` 후 `flush()` 누락으로 영역 지정 직후 앱 종료 시 오버라이드가 사라지던 문제 해소(보정/창 소스 저장과 동일 규칙).
- 회귀망: `test/ocr/bar-fill-calibration.test.ts` (d) 블록(실측 저채도 게이지 보정 성공 + 부분 채움 측정 + 갈색·청회색·보라·노이즈복권 거부 보존), `test/debug/dbg-mpcal-sat-gate.test.ts`(필드 시나리오 4종 — 수정 전 재현 실험을 수정 후 기대치로 전환). 테스트 36파일 278개 PASS · typecheck PASS.

### UX/UI 개선 (5방향 발굴 24건 중 TOP 5 구현)

- **프리셋 저장 복구** (`setup.ts`): `window.prompt()`는 Electron 렌더러에서 지원되지 않아 "현재 설정 저장"이 통째로 죽어 있었음 — 인라인 이름 입력칸(Enter 지원)으로 교체 + 저장/삭제에 `flush()` 추가(앱 종료 시 증발 차단).
- **전역 단축키 가드 확대** (`app.ts`): INPUT/SELECT만 제외하던 가드에 TEXTAREA(템플릿 JSON 편집 중 R키=세션 리셋 사고)와 BUTTON(포커스 시 Space는 표준 활성화)을 추가 — `closest('input, select, textarea, button')`.
- **ROI 편집기 fit-to-width 줌** (`setup.ts`): 네이티브 1:1 스냅샷의 가로 스크롤이 "다른 파란 UI를 게이지로 오인 드래그"를 유발하던 함정(6/7 실측) — 불러오기 시 기본 전체-폭 맞춤(「↔ 맞춤」/「100%」 토글), 좌표는 rect 역산으로 줌 무관 정확. + Esc 드래그 취소, 4px 미만 드래그 시 안내.
- **ROI 저장 즉시 크롭 미리보기** (`setup.ts`): 지정한 영역의 실제 크롭 이미지를 상태줄 옆에 표시 — 오인 드래그를 "한참 뒤 보정 실패"가 아니라 즉시 발견. MP 바는 파랑-우세 픽셀 비율 힌트(≥30% ✓ / 미만 경고) 부가(보조 신호, 판정 권위는 보정).
- **보정 실패 reason별 맞춤 안내** (`detection.ts`+`setup.ts`): `calibrateMpBar`가 reason(`not_blue`/`no_fill_color`/`not_full`/`empty_roi`/`no_region`/`capture_fail`)과 자기검증 selfRatio를 반환 — 영역 문제면 「🎯 MP 바 영역 다시 지정」 원클릭 버튼(편집기 자동 오픈+MP 바 선택+스크롤), 미충전이면 "100% 채운 뒤" 안내+재시도 버튼, 성공 시 자기검증 % 표시(비정상이면 ⚠ 재보정 권고 — 로그에만 있던 정보의 표면화).

## v3.1.0 — 2026-06-07

OCR 정확도/추적 종합 수정 릴리스. 픽스처 6-에이전트 진단 + 라이브 진단(`.omc/research/ocr-diagnosis-{fixtures,live}.json`)에서 도출한 근본원인(RC-A~F)을 전부 코드로 해소. v3.0.0 이후의 모든 Unreleased 항목(아래 ### 절들) 포함.

### OCR 진단 라운드 — 정확도 실측 (수정 전 → 후)

| 영역 | 수정 전 (기존 i%2 분할) | 수정 후 (같은 i%2 분할) | 수정 후 (정직한 세션-홀드아웃, 신규 기준) |
|---|---|---|---|
| exp | 54.5% | 100% | **98.2% (54/55)** |
| level | 71.4% | 95.2% | 0/1 — n=1 정보성 (코퍼스 라벨 2종뿐, 데이터 필요) |
| adena | 5.5% | 70.9% | **73.0% (27/37)** |
| mp(텍스트) | 0% (측정무효) | — | 측정무효 유지 (픽스처가 v2 그레이스케일 산물 — 라이브 1순위는 바-픽셀) |

테스트 138 → **270개**(35파일) 전부 PASS · typecheck(node+web) PASS. 기존 i%2 분할은 같은 캡처 버스트의 인접 프레임을 train/test 양쪽에 흘려(레벨은 test 라벨의 95%가 train에도 존재) 낙관 편향이 있었으므로, 세션-그룹 홀드아웃 수치가 새 공식 기준.

### MP 보정 검증 + flush (`bar-fill.ts` / `detection.ts` / `setup.ts`)
- **거짓 성공 보정 차단**: 287×47 과대 ROI에서 갈색 UI 패널 `rgb(111,92,79)`를 채움색으로 학습하고도 "보정 완료(277 cols)"가 뜨던 현장 사고 — `calibrateBarChecked`가 ① 파란-우세 행 밴드로 ROI 자동 축소(shrink-to-band), ② 채움색 파란-우세 게이트(아닌 색은 사유+감지색과 함께 거부), ③ ≥50% 채움 게이트(100% MP에서 보정), ④ 같은 ROI 재측정 자기검증(ratio 0.95~1.0)을 수행. 기존 저장된 비-파랑 보정값도 로드 시 무효화(보정 필요 상태로 전환).
- **보정 증발 수정**: 보정/창 소스 저장이 persistence 스케줄을 안 타던 raw `store.set` 변이에 `flush()` 추가 — 인식 루프 정지 중 보정 후 앱 종료 시 보정이 조용히 사라지던 문제(6/4 현장 패턴) 해소.
- **무보정 텍스트 폴백 차단**: 바-픽셀 모드 ON + 유효 보정 없음이면 텍스트 OCR 폴백 자체를 차단(`calibration_required` 이벤트 + "MP 바 보정 필요" 힌트). 작은 cur/max 텍스트가 "000000"(conf 0.66)으로 오독돼 cur=0이 앵커로 채택되던 라이브 사고의 진입로 봉쇄.
- **색 스와치 UX**: 보정 결과에 학습된 색을 스와치+rgb로 표시 — 갈색 스와치면 영역이 게이지를 벗어났음을 즉시 확인 가능.
- 슬래시 없는 MP 텍스트 파싱 게이트: `cur > maxMp×2`면 무효 처리, 그 외엔 conf×0.6 감쇠(HUD는 항상 "cur/max"라 '/' 미인식 자체가 구조적 의심 신호).

### tracker freeze 해소 (`tracker.ts`)
라이브 진단: ① 수동입력 10분 잠금 → 해제 시점 실값과 Δ>10%p → 영구 `low_posterior` 거부 체인(B3), ② 앵커 오염 후 "연속 동일값 N회" 복구 게이트가 계속 변하는 EXP에 구조적 불능(B1), ③ 라이브 conf 0.72에서 Δ>10 교정이 수학적으로 불가 + EXP/LEVEL temporal이 경과시간 무시(D)의 3중 freeze.
- **stale-anchor 재부트스트랩**: 수락 없는 거부가 45초 지속되면 앵커를 폐기하고 init 부트스트랩(N연속 동일 + conf 게이트)으로 복귀 — 어떤 극단 이탈도 ~48s 내 자가 복구(실측 R5).
- **수동 잠금 600s → 90s** + 조기 해제: 잠금 중 OCR이 입력값과 3연속 일치(EXP ±1%p, LEVEL 정확일치)하면 즉시 해제 — 자동 인식이 같은 숫자를 읽음이 입증되면 잠금 유지 무의미.
- **dt-완화 temporal**: EXP 밴드가 ~0.2%p/s로, LEVEL 예산이 분당 1레벨로 경과시간에 따라 확장(MP의 rate=|Δ|/dt와 동형) — 앵커가 정지한 동안 누적된 정당한 사냥 진행분이 시간이 갈수록 수락 가능해짐. dt≤5s(라이브 1s 틱)에선 기존 밴드와 동일.
- **off-by-one 교정**: anomaly 경로의 record/check 순서를 init 경로와 통일 — 승급이 설계값 N프레임(기존 N+1)에 정확히 발동(실측 R4).
- **init conf 게이트**(기본 0.5): 부트스트랩 일관성 창에 저신뢰 쓰레기("000000")가 카운트되지도, 창을 끊지도 않음 — 쓰레기가 앵커를 시딩 불가(실측 R6).
- **ADENA rate 하한**: 1.0/s → `max(5000, 앵커×0.5)`/s — idle(rateEma≈0) 후 첫 픽업이 6프레임 지연 없이 1프레임 수락, EXP는 1.0 유지로 오독 방어 보존(실측 R8/진단 E).
- 회귀 테스트 9종(`test/ocr/tracker-freeze-regression.test.ts`).

### 템플릿 오염 차단 (`template-matcher.ts` / `learn.ts` / `base-templates.json`)
진단 RC-A: 오정렬 MP 픽스처의 54×2 게이지 띠 8개가 '1'로 학습돼 meanAspect 6.26(정상 0.69) → 진짜 '1'마다 aspect 페널티 포화(0.15) → "자신있는" 1↔7 혼동 15건(exp 실패 25건 중 15건).
- TemplateBuilder aspect 통계를 산술평균 → **중앙값**(소수 이물질에 강건, 깨끗한 샘플에선 평균과 동일 거동, 직렬화 필드명 `meanAspect` 유지).
- 학습 시 **기하 가드** `digitGlyphsPlausible`(digit 한정 — '.'·','·'/'는 면제 필수, 면제 누락 시 70/80 픽스처 탈락 실측): 형제 digit 중앙값 절반 미만 높이 / aspect>1.4 / 마스크 높이 90%↑ chrome 메가글리프면 **샘플 전체 거부**. 프로덕션 `learnFromCapture` + 벤치 빌더 + 일괄학습 스크립트가 전부 동일 가드 공유.
- 유저 템플릿 병합 last-writer-wins → **샘플 가중 평균**(`mergeTemplateSetsWeighted`, 신규 학습 가중치 ≥50% 보장): 반복 학습이 통계적으로 누적(v2 코퍼스 실측: 248-샘플 평균이 단발 캡처 대비 +5.4pp)되면서 "다시 가르쳐 즉시 교정" UX 유지.
- `base-templates.json` 재생성: '1' meanAspect 6.36→0.69, 100% 오염이던 '/'(4×2 슬리버) 템플릿 제거. `gen-base-templates`에 digit aspect 0.3~1.4 회귀 가드 추가.
- 학습 채택 캡처의 라벨 코퍼스 저장 재개(`saveTrainingSample` IPC 재연결 — v3에서 호출부가 누락돼 코퍼스 수집이 멈춰 있었음).

### 세그멘테이션 교정 (`segmentation.ts` / `text-recognizer.ts`)
- x-겹침 병합 **전에** 납작한 가로 슬리버(레벨 박스 테두리·게이지 띠 등 chrome) 제거 — chrome이 디지트들을 연쇄 병합해 단일 메가글리프로 만들던 문제(레벨 "28" → 1글리프) 수정. '.'(정사각)·'/'·'1'(세로형)은 구조상 안전.
- 붙은 글자 분할: 트리거 추정을 패딩 포함 mask 높이(~46% 세로 패딩으로 1.85× 과대) → **잉크 높이 중앙값**으로, 분할 개수는 박스 폭 중앙값(≥3박스 시)으로 — '4X' 붙음 블롭(w=41)이 참 경계(90/91)와 0.5px 차로 분리.
- `removeSolidBands` rowFrac 영역별 분리: **level만 0.6**(박스 chrome 제거 — 71.4%→95.2%, 기존 i%2 기준), 나머지는 0.8 유지(전역 0.6은 exp 7.3%/adena 0% 붕괴 실측).

### adena 전처리 체인 (`text-recognizer.ts` / `segmentation.ts`)
- **2-pass 텍스트-밴드 사전 크롭**: 744×492처럼 세로로 큰 캡처에서 `scaleToHeight(48)`가 숫자를 ~13px로 뭉개던(33151→"7777") 문제 — 1차 분할로 글리프 과반이 덮는 행 밴드를 찾아 원본 행으로 역매핑(±2px), 그 크롭만 재이진화. 처방 체인 합계 실측 5.5%→72.7%(픽스처), 세션-홀드아웃 73.0%.
- **중앙값-상대 정크 글리프 필터**(adena 한정 opt-in): 경계 접촉 + 중앙값 폭 미만(인벤토리 UI 조각 13×35px) / 납작 노이즈 / 희소 스펙클 제거. 학습 경로도 동일 체인 사용(미사용 시 train 활용 12/55 → 사용 시 27/55).
- **신뢰도-가드 끝-간격 트림**: 글리프 간격 >3×중앙값 **이고** conf<0.7인 끝 글리프만 제거 — 무조건 트림은 커닝 불균일에서 실제 끝자리를 자름(실측 28491→"2849" 1건 방지).
- adena 우선 템플릿 셋(자기 코퍼스 글자 우선, 공유 셋 폴백 — level과 동일한 merge 의미론).

### 벤치 위생 (`test/helpers/fixtures.ts` / `accuracy.test.ts`)
진단 RC-E: 기존 `i % 2` 분할이 같은 캡처 버스트의 인접 프레임을 train/test에 흘림(level test 라벨의 95%·mp 84%가 train에도 존재) — 모든 수치에 낙관 편향.
- **세션-그룹 홀드아웃** 도입: 파일명 타임스탬프 체인 + 동일 GT 라벨 단위로 그룹화해 그룹 통째로 한쪽에만 배치. 경계 재기준선: exp>0.95, adena>0.65 단언; level(라벨 2종, n=1)·mp(도메인 무효)는 정보성.
- adena **라벨-크롭 불일치 dunder 배치 29쌍 격리**: `test/fixtures/ocr/adena/_quarantine/`로 이동(로더는 비재귀라 미적재), `fixture-hygiene.test.ts`로 보존+미적재를 고정.
- 공유 템플릿 셋에서 mp(v2 그레이스케일 쓰레기 세그먼트)·level(다른 래스터 17×20 — 공유 시 exp 100%→70.9% 교차 오염 실측) 제외, level/adena는 자기 폰트 셋 우선.

### v2 라벨 코퍼스 일괄 학습 (`scripts/build-user-templates.ts` / `settings.ts`)
- v3에 v2 시절 라벨 코퍼스(`%APPDATA%/LineageMPTimer/training-data`, 2,600+ 파일)를 읽는 코드 경로가 전무하던 문제(라이브 진단 #3) — **오프라인 일괄 학습기** 신설: `npm run templates:user` → `out/user-templates.json`. exp 한정(실측 +7.3pp; level은 0pp+교차 오염, adena는 세그 단계 문제라 제외), 프로덕션과 동일한 정렬+기하 가드 통과 샘플만 사용.
- 환경설정 탭 「**OCR 유저 템플릿**」 드로어 신설: 산출물 JSON 붙여넣기 → 구조 검증 → 가져오기(가져온 글자 우선, 라이브로 학습한 다른 글자는 보존) / 초기화. 저장 즉시 detection 재로드.

### (v3.0.0 이후 누적 — 함께 릴리스)

#### MP 바 픽셀 측정 근본 robustness (실사용 MP 인식 실패 수정)

진단(실기 로그): MP가 줄어도 계속 `320/320`(가득 참)에 고착되고, 가끔 `26/320`으로 거짓 급락 → 거부/오락가락. "점검할 땐 잘 되는데 막상 보면 MP 인식이 안 됨"의 정체. 두 가지 코드 레벨 근본 원인:

- **고착(saturation) 수정** (`bar-fill.ts`): 보정된 채움색이 `rgb(110,91,80)`(갈색)인데 빈 게이지(트랙)도 비슷한 갈색이라, **고정 허용오차 70 하나로는 빈 컬럼도 "채워짐"으로 카운트** → MP가 줄어도 바가 항상 가득 참으로 측정됨(보정 *전* 310→보정 *후* 320 고착이 결정적 증거). 이제 바 **우측 끝에서 빈-트랙 색을 자동 샘플링**하고, 각 컬럼을 *상대 거리*(채움색에 더 가까운가 vs 트랙색에 더 가까운가)로 분류 → 두 색이 비슷해도 분리. 바가 가득 차 빈 샘플이 없으면(트랙≈채움) 자동으로 절대-허용오차로 폴백(가득 참 정상 측정). 기존 보정값 그대로 사용(**재보정 불필요**).
- **순간 가림 거짓 급락 수정** (`bar-fill.ts`): 스킬 이펙트·플로팅 데미지·몬스터가 바를 가리면 채움 중간에 구멍이 생김. 기존엔 왼쪽부터 스캔하다 빈 컬럼 4개 초과 시 `break` → 구멍 앞에서 잘려 거짓 near-empty(`24컬럼≈26/320`). 이제 **오른쪽에서부터 "국소적으로 채움이 우세한 최우측 경계"를 탐색** → 내부 구멍이 측정을 자르지 못하고(가림 무시), 우측의 고립된 노이즈 컬럼도 국소밀도 게이트로 거름. 320→26→320이 2초 만에 일어날 수 없음(자연회복 한계)이 거짓 급락의 증거였음.
- **MP 트래커 응답성**: 전투 중 MP는 빠르게 변하고 bar-pixel은 고신뢰(0.95)이므로, 진짜 변화의 anomaly 승격 기준을 5→**3회 일관**(~3초)로 단축 — 표시 MP가 실제를 더 빨리 따라감.
- 신규 테스트 5종: 트랙≈채움색 비고착·다단계 추적·내부 가림 무시(가득/절반)·우측 노이즈 무시. 전체 138개 통과.

#### 학습 힌트가 "실제 사용값" 표시 (혼란 수정)

- **"인식:" 힌트가 거짓말하던 문제 수정**: 값이 잠금/거부 상태여도 힌트가 *무시되는 라이브 OCR*을 표시 → "내가 입력한 값과 다른 값을 쓰는 것처럼" 보였음. 실제로는 앱이 입력값을 정상 사용 중이었음. 이제 힌트는 **앱이 실제 사용하는 값**을 표시하고(`인식:`=채택 / `사용: X · OCR Y(무시)`=거부·잠금), 무시되는 OCR 읽기는 진단용으로 함께 노출. 수동 입력 즉시 반영.
- **ROI 편집기 "직접 지정됨" 상태 표시**: 어떤 항목에 수동 영역이 지정됐는지 한눈에 확인.
- **아데나 쉼표 안내**: 화면이 `31,525`처럼 쉼표를 포함하면 학습 시 쉼표까지 입력해야 글자수가 맞음(안내 + 예시 추가). 학습 실패 메시지를 정직하게(실패 사유 노출, "정상입니다" 오문구 제거).

#### 창 모드 영역 직접 지정 편집기

- **영역 직접 지정(ROI) 편집기 추가** (창 모드, 설정 → 화면 인식 → 「🎯 영역 직접 지정」): 「📷 게임 화면 불러오기」로 게임 창 스냅샷을 띄우고, 항목(MP 숫자/MP 바/EXP/레벨/아데나)을 골라 이미지 위에서 마우스로 드래그하면 해당 영역을 직접 지정. 지정한 영역은 자동 검출 대신 그대로 사용됨(`autoDetect.windowRoi` 오버라이드, 창-프레임 물리px 1:1). 자동 ROI가 EXP/레벨/아데나를 잘못 잡던 클라이언트도 MP처럼 정확하게 만들 수 있음. 「선택 영역 자동으로」로 개별 해제.
- 스냅샷은 창 네이티브 해상도로 1:1 표시(스크롤) → 그린 좌표가 곧 캡처 좌표라 오차 없음. 저장 즉시 detection이 재반영(`forceRoiRefresh`).
- 스키마 v2→v3 마이그레이션(`windowRoi`), 기존 사용자 무영향. 사용 흐름: 영역 지정 → 「보정·학습」에서 글자 학습 → 정확.

#### 수동 입력값 잠금 (OCR이 덮어쓰던 문제)

- **직접 입력한 값을 OCR이 즉시 덮어쓰던 문제 수정**: `force`(수동 입력)에 시간 잠금이 없어 다음 틱의 OCR(오인식 포함)이 곧바로 덮어썼음. 이제 EXP/레벨/아데나는 수동 입력 후 일정 시간 OCR 무시(값 고정), MP는 제외(bar-pixel이 정확하고 MP는 계속 변함). tracker에 `manualLockMs` + `user_locked` reason 추가, 단위 테스트 3종. (최초 10분이었으나 freeze 체인의 원인으로 진단돼 본 릴리스에서 **90s + 3연속 일치 조기해제**로 단축 — 위 "tracker freeze 해소" 참고.)
- **알려진 한계**: 창 모드 자동 ROI가 일부 클라이언트에서 EXP/레벨/아데나 영역을 잘못 잡음(레벨 6글자·아데나 10글자 등) → 자동 인식 부정확. 근본 해결은 "게임 창에 영역 직접 지정 편집기"(예정). 그 전까지는 수동 입력(이제 10분 고정)으로 사용 가능. MP는 자동 정확.

#### 단축키 비활성화 버그 수정

- **전역 단축키(F1/F2) 끄기가 적용 안 되던 버그 수정**: ① main이 시작 시 `DEFAULT_HOTKEYS`(F1/F2 ON)를 무조건 등록하는데 렌더러가 **부팅 시 저장된 enable/disable 상태를 동기화하지 않아**, 껐다가 재시작하면 다시 켜졌음 → 부팅 시 `setGlobalHotkeys` 동기화 추가. ② `setGlobalHotkeys` 재등록이 `pushEvent('hotkey')`로 라우팅됐는데 렌더러는 이 이벤트를 구독하지 않아 재등록된 F1/F2가 무반응이 되던 잠재 버그 → main-side `dispatchHotkey`로 일원화(시작·재등록 동일 경로).
- **레벨 수동 입력 안내 정리**: 창 모드 자동 ROI가 레벨 영역을 과분할(6글자)해 자동 인식이 어려운 점을 반영 — 레벨은 보정·학습에서 직접 입력하면 OCR 오인식이 덮어쓰지 않고 유지됨(메시지를 정직하게 수정).

#### UI 가독성/폰트 + EXP 표시 정리

##### UI
- **입력칸 텍스트 안 보임 수정**: 보정·학습/아이템 입력칸이 `.field` 밖(`.row`)이라 테마 미적용 → UA 흰 배경 + 밝은 글자(=안 보임)였음. 전역 폼 컨트롤 테마(어두운 배경·밝은 글자·placeholder·focus, WCAG AAA 대비)로 수정. 키보드 `:focus-visible` 링은 유지(a11y).
- **버튼 크기 축소**: `.btn` 패딩 8/16→8/12, 글자 15→13px(`--sm` 11px), nowrap — 전반적으로 덜 투박하게.
- **보정·학습 행 간격**: `.card` 직속 행들이 세로로 붙던 문제 → `.stack` 유틸 + 학습행 2줄 구조(라벨+입력+버튼 / 상태+힌트), 입력칸 flex.
- **폰트 교체**: 시스템 폴백(맑은 고딕) → **Pretendard**(variable woff2 번들, 오프라인). 빌드 전용(devDependency)이라 exe 크기 영향 최소(+2MB).

##### EXP 표시/학습
- **`인식:` 힌트가 정규화값 표시**: raw `79.3390232`(끝 "%"가 3조각 과분할) 대신 파서 정규화값 `79.3390` 표시(raw는 툴팁). 추적값은 원래부터 정상이었고 표시만 raw여서 생긴 혼란 해소.
- **EXP 학습 과분할 허용**: `learnFromCapture`가 EXP 한정 좌측 N글자만 학습(숫자는 좌측, "%" 노이즈는 우측). 다른 영역은 strict 유지.
- 창 모드 학습 실패 안내 문구 정정(영역 재지정 불가 → "자동 영역, 값 인식 정상").
- `인식값↩`: EXP는 정규화값, 그 외는 raw(콤마/슬래시 글자수 보존)로 채움.

#### 듀얼 모니터 핫픽스 + 게임 창 자동 캡처

##### 듀얼 모니터 / 고배율(HiDPI) 캡처 버그 수정
- **이중 스케일링 제거** (`screen-capture.ts`): 오버레이가 영역 지정 시 이미 논리→물리 픽셀로 변환해 저장하는데, 캡처 시 `scaleFactor`를 한 번 더 곱해(`region.x * sf`) 배율 ≠ 100% 디스플레이에서 크롭이 빗나가던 버그. 100% 모니터는 무해(1×1)했으나 125/150% 보조 모니터에서 영역이 어긋나 OCR 실패. 저장 좌표를 물리 픽셀로 통일하고 캡처 시 재곱셈 제거.
- 데스크톱 캡처 해상도 상한 4096/2160 → **7680/4320** (4K↑/듀얼-4K에서 다운스케일로 물리=비디오 가정이 깨지던 위험 제거).

##### 게임 창 자동 캡처 모드 (모니터 무관)
- **캡처 방식 선택**: 「화면 영역(수동)」 ↔ 「게임 창(자동)」. 창 캡처는 영상 원점이 창 좌상단이라 **어느 모니터에 있든, 창을 옮겨도** ROI가 유효 — 듀얼 모니터 좌표 매칭 문제 자체가 사라짐.
- **창 제목 자동 매칭**: `listWindows`로 창 목록 → `lineage|리니지` 자동 선택. 창 `window:HWND` id는 게임 재시작마다 바뀌므로 매 시작 시 **제목으로 재해결**(`resolveWindowSource`).
- **자동 ROI 검출**: `detectGameUiScaled`가 창 전체 프레임에서 HP/MP/EXP/레벨/아데나 ROI를 배율 정규화(기준 1280폭 다운스케일→검출→원본 px 복원)로 자동 도출. 창 ROI는 메모리 보관(영속 스토어 미오염, scaleFactor=1).
- **견고성**: 검은 화면(전체화면 독점) 감지 + 안내, 창 최소화/소실 시 `roiFailThreshold` 초과하면 자동 재탐색(self-heal), 「게임 창 탐색 중…」 상태 표시.
- 저장 스키마 v1→v2 마이그레이션(`captureMode`/`windowId`/`windowTitle`), 기존 화면-영역 사용자 무영향.
- 신규 IPC: `listWindows`, `resolveWindowSource`. 신규 테스트: 합성 풀프레임 ROI 검출(1×/2× 배율·디코이·얇은 EXP·검은 프레임), 스키마 마이그레이션.

## v3.0.0 — 2026-06-05

전면 리뉴얼. JavaScript 모놀리식(8268줄 `app.js`) → **TypeScript + electron-vite** 모듈 구조.

### OCR(문자 인식) 근본 재설계
v2.x의 간헐 오인식(`0↔8`, `4↔9`, `6↔8`) 근본 원인을 제거:

- **등폭 글자 분할 폐기 → 연결성분(CCL) 분할**: 잉크의 실제 연결 구조로 글자 경계를 찾음 (한 컬럼 어긋남이 옆 글자를 오염시키던 #1 원인 제거)
- **MP 바 픽셀 측정**을 MP 1순위로: 게이지 채움 컬럼 수 직접 측정 → ~100% 정확, OCR 불필요 (100% 보정 1회 필요)
- **유저 디지트 템플릿 학습**: 실제 값 입력 → 본인 게임 폰트 학습 → 해당 글자는 항상 정확 (학습된 글자는 기본 템플릿으로 폴백 안 함)
- **베이지안 시간추적 거부권**: v2.x DRY-RUN(관찰만) → 실제 거부권 부여
- **EXP 소수 4자리 정규화**: 끝의 `%` 글자가 가짜 5번째 소수로 잡히던 문제 자동 보정 (`78.47472` → `78.4747`)
- 캔버스/DOM 독립 `RgbaImage` 파이프라인 → 게임 없이 1,300여 캡처 샘플로 정확도 단위 테스트

### UI/UX
- 4탭(개발자 콘솔 노출) → **3탭** (모니터 / 설정·OCR / 환경설정)
- 디자인 토큰 시스템 (v2.x 미정의 `--accent` 등 해결, 시각언어 통일)
- 카운트다운 히어로 중심 시각 계층, 진단은 숨김 드로어로
- 접근성: ARIA, `prefers-reduced-motion`, 테마 스크롤바
- 컴팩트 HUD 재설계

### 진단
- 중앙 로그 시스템 (`고급·진단` 드로어 / DevTools `lmpLog`), 로그 복사·저장
- 영역별 인식 결과(원문/파싱/채택/거부사유) 실시간 기록

### 인프라
- 단일 reactive store (DOM-as-anchor 안티패턴 제거)
- 타입 안전 IPC 계약, Electron main 8개 모듈 분리, async fs
- 저장 스키마 버전 관리(ad-hoc 마이그레이션 플래그 제거)
- vitest 112개 테스트, node/web 타입체크 통과

### 정리
- 구버전 빌드 산출물(v1.8.4, win-unpacked ~400MB) + 죽은 스크립트 + 모놀리식 소스 제거
- Paddle/Tesseract voting 은퇴, 미사용 traineddata 번들 제외
