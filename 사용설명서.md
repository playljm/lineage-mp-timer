# 🎮 리니지 MP 타이머 v3.0 — 사용 설명서

> 게임 화면을 자동 인식해 **MP 완충 시간 / EXP·h / 아데나·h / 레벨업 ETA**를 계산합니다.
> v3.0은 문자 인식(OCR)을 근본부터 다시 만들고 UI를 3탭으로 정리했습니다.

---

## 🆕 v3.0 핵심

- **MP는 바 픽셀 측정이 1순위** — MP 게이지의 채워진 길이를 직접 재서 ~100% 정확 (숫자 OCR 불필요).
- **내 폰트 학습** — 본인 게임 글자를 학습하면 그 글자는 항상 정확. 학습된 글자는 기본 템플릿으로 절대 되돌아가지 않음.
- **글자 분할 방식 교체** — 잉크의 실제 연결 구조로 글자 경계를 찾아, v2.x의 `0↔8 / 4↔9 / 6↔8` 혼동의 근본 원인을 제거.
- **시간 추적 검증(거부권)** — 갑자기 튀는 값은 추적기가 실제로 거부 (이전엔 관찰만 했음).
- **3탭 UI** — 모니터 / 설정·OCR / 환경설정. 진단 패널은 "고급·진단" 드로어로 숨김.

---

## 🚀 처음 1분 셋업

1. **리니지 실행** — 창모드 또는 테두리 없는 창 (전체화면 ❌), MP/EXP/레벨/아데나가 보이게.
2. 타이머 실행 → **설정·OCR 탭**.
3. "캐릭터 / MP"에서 **최대 MP**와 **WIS** 입력 (이게 있어야 자동 계산됨).
4. "화면 인식(OCR)"에서 영역 지정:
   - 가장 정확한 조합: **📊 MP 바** 영역 + **MP 바 픽셀 모드 ON** (MP), 그리고 **EXP / 레벨 / 아데나** 숫자 영역.
   - 영역 지정 버튼 → 게임 화면에서 드래그 (크로스헤어 + 5배 루페 확대경으로 픽셀 단위 조정).
5. **▶ 자동 인식 시작**. 상태 줄에 "실행 중" 표시.
6. **모니터 탭**에서 완충 카운트다운 + 트래커 확인.

---

## 🖥️ 탭 구성

| 탭 | 내용 |
|---|---|
| **모니터** | MP 완충 카운트다운(히어로), MP 게이지, 시작/일시정지/리셋, 회복량 분해, 세션 트래커(EXP/레벨/아데나, /h, 레벨업·다음 1% ETA) |
| **설정·OCR** | 캐릭터/MP 입력, 프리셋, OCR 영역 지정, MP 바 픽셀 모드, 아이템 판매 합산, "고급·진단" 드로어 |
| **환경설정** | 테마(4종), 알림(사운드/토스트/볼륨), 항상 위, 단축키 재바인딩, 클라우드 동기화 |

---

## 🪟 컴팩트 모드

- 우측 상단 **📦** 버튼 또는 **F3** — 게임 위에 작은 HUD로 완충 카운트다운 + 핵심 지표만.

## ⌨️ 단축키

| 키 | 동작 |
|---|---|
| `Space` | 시작/일시정지 |
| `R` | 리셋 |
| `F1` | 항상 위 (전역) |
| `F2` | 창 숨기기 (전역) |
| `F3` | 컴팩트 모드 |

환경설정 탭에서 전부 재바인딩 가능.

---

## ❓ 자주 묻는 질문

**Q. MP 인식이 가끔 틀려요**
→ "MP 바 픽셀 모드"를 켜세요. 숫자 OCR 대신 게이지 길이를 재서 가장 정확합니다.

**Q. EXP/아데나가 가끔 튀어요**
→ 시간 추적기가 대부분 거부하지만, 영역을 숫자에 딱 맞게(여백 최소) 다시 지정하면 더 안정적입니다. 그래도 틀리면 모니터/설정에서 직접 입력하면 우선 적용됩니다.

**Q. 아데나가 "10만" 한글 단위로 보여요**
→ 게임이 큰 수를 한글 단위로 압축 표시하면 OCR 불가입니다. 직접 입력하거나 "아이템 판매 합산"으로 누적하세요.

**Q. 잘못된 값이 굳었어요**
→ 직접 입력한 값은 추적기가 강제 채택하므로, 모니터/설정에서 올바른 값을 입력하면 즉시 교정됩니다.

**Q. 진단 정보는 어디서 봐요?**
→ 설정·OCR 탭 → "고급·진단" 드로어. 최근 인식 결과(영역/원문/채택여부/신뢰도)와 학습된 디지트(0~9) 진척을 보여줍니다.

---

즐거운 사냥하세요! 🗡️
